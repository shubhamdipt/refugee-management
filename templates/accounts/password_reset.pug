extends ./../layout.pug
block content
    .container
        .row
            .col-sm-12
                h1 Reset password
        .row
            .col-sm-6
                form(method="POST")
                    | {% csrf_token %}
                    | {{ form|crispy }}
                    button.btn.btn-success(type="submit") Submit
